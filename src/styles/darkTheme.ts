import { createTheme } from "@mui/material";

const darkTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#424242",
    },
    secondary: {
      main: "#ffffff",
    },
  },
});

export default darkTheme;
